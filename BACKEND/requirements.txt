# Core
django>=4.2,<5.0
djangorestframework>=3.14
django-cors-headers>=4.3

# Database drivers — install the one you need:
# For XAMPP (MySQL):
#   pip install mysqlclient
# For SSMS (SQL Server):
#   pip install mssql-django pyodbc

# Optional: for testing the DB connection utility
# pyodbc        (only if using SQL Server)
# mysqlclient   (only if using MySQL / XAMPP)
