# VITE_API_BASE_URL: The base URL for the Django backend API.
# In development, the Vite proxy forwards /api to Django on port 8000.
# In production, update this to your deployed Django server URL.
VITE_API_BASE_URL="/api"
